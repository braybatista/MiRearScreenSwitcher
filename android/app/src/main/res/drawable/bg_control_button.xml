<?xml version="1.0" encoding="utf-8"?>
<!--
Este drawable crea un efecto de onda (ripple) para los botones.
Funciona independientemente del tema de la aplicación, solucionando el crash.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#4DFFFFFF"> <!-- Color de la onda (blanco con ~30% de opacidad) -->

    <!-- Máscara: Define la forma de la onda. 'oval' es perfecta para botones redondos. -->
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

</ripple>

